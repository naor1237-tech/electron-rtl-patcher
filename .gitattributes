# PowerShell scripts must keep CRLF + be treated as text
*.ps1 text eol=crlf
*.psm1 text eol=crlf
*.json text eol=lf
*.md text eol=lf

# Keep the RTL payload bytes stable so any future signature stays valid
lib/rtl-inject.js text eol=lf
